import { GetSlugByIdPipe } from './get-slug-by-id.pipe';

describe('GetSlugByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSlugByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
