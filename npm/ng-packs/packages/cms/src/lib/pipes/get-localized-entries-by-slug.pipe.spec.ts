import { GetLocalizedEntriesBySlugPipe } from './get-localized-entries-by-slug.pipe';

describe('GetLocalizedEntriesBySlugPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLocalizedEntriesBySlugPipe();
    expect(pipe).toBeTruthy();
  });
});
